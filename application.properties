spring.application.name=JOB-SERVICE-Default

# Git config server settings (already present)
# spring.cloud.config.server.git.uri=...

# Shared Zipkin tracing
spring.zipkin.base-url=http://localhost:9411
spring.sleuth.sampler.probability=1.0           # if using Spring Cloud Sleuth
# Or, for Spring Boot 3.x + Micrometer:
management.tracing.sampling.probability=1.0
management.zipkin.tracing.endpoint=http://localhost:9411/api/v2/spans



